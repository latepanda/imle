{
  "name": "imle",
  "private": true,
  "type": "module",
  "scripts": {
    "check:mercury-utilities": "node src/generate-mercury-utilities.mjs --check",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
